export interface BasicDetails {
    name: string;
    address: string;
    city: string;
    dob: Date | null;
  }
  
  export interface AdvancedDetails {
    occupation: string;
    phoneNumber: string;
  }
  